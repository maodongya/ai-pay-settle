# 全方位监控方案

> 日期：2026-07-16  
> 目标：对 **MySQL、Redis、RocketMQ、ai-pay-settle（pay-app）** 及宿主机/容器资源进行全方位监控，指标统一写入 **pay-prometheus**，在 **pay-grafana** 可视化。

---

## 1. 架构

```
┌─────────────┐   scrape    ┌──────────────────┐   query    ┌─────────────┐
│ Exporters   │ ──────────► │ pay-prometheus   │ ─────────► │ pay-grafana │
│ + pay-app   │   15s/10s   │ :9090 (TSDB 30d) │            │ :3000       │
└─────────────┘             └──────────────────┘            └─────────────┘
```

| 层级 | 组件 | 采集方式 | Grafana 看板 |
|------|------|----------|--------------|
| 宿主机 | node-exporter :9100 | CPU/内存/磁盘/网络 | 基础设施总览 |
| 容器 | cAdvisor :8082 | 各容器 CPU/内存 | 基础设施总览 |
| MySQL | mysqld-exporter :9104 | 连接数/QPS/InnoDB | 基础设施总览 |
| Redis | redis-exporter :9121 | 内存/客户端/OPS | 基础设施总览 |
| RocketMQ | rocketmq-exporter :5557 | Broker TPS/消费滞后 | 基础设施总览 |
| 应用 | pay-app :18089 `/actuator/prometheus` | 业务 TPS/积压/JVM/HikariCP/MQ Lag | 业务总览 + 运行时 |

**数据流**：Exporter / Actuator → Prometheus TSDB → Grafana（Datasource 已预置 `http://prometheus:9090`）。

---

## 2. 监控指标清单

### 2.1 宿主机（node-exporter）

| 指标 | PromQL 示例 | 告警阈值 |
|------|-------------|----------|
| CPU 使用率 | `100 - avg(rate(node_cpu_seconds_total{mode="idle"}[5m]))*100` | > 85% 持续 5m |
| 内存使用率 | `(1 - node_memory_MemAvailable_bytes/node_memory_MemTotal_bytes)*100` | > 90% |
| 磁盘可用 | `node_filesystem_avail_bytes{mountpoint="/"}/node_filesystem_size_bytes*100` | < 15% |

> macOS + Docker Desktop 下 node-exporter 反映 Linux VM 资源，仍可用于本地联调趋势观察。

### 2.2 容器（cAdvisor）

| 指标 | PromQL 示例 |
|------|-------------|
| 容器 CPU | `rate(container_cpu_usage_seconds_total{name=~"mysql8|redis7|rmq-.*|pay-.*"}[5m])` |
| 容器内存 | `container_memory_usage_bytes{name=~"mysql8|redis7|rmq-.*|pay-.*"}` |

### 2.3 MySQL（mysqld-exporter）

| 指标 | 说明 |
|------|------|
| `mysql_global_status_threads_connected` | 当前连接数 |
| `rate(mysql_global_status_questions[5m])` | QPS |
| `mysql_global_variables_max_connections` | 最大连接 |
| `mysql_global_status_innodb_buffer_pool_reads` | Buffer Pool 读盘 |

连接使用率告警：`threads_connected / max_connections > 0.8`

### 2.4 Redis（redis-exporter）

| 指标 | 说明 |
|------|------|
| `redis_memory_used_bytes` | 已用内存 |
| `redis_connected_clients` | 连接客户端数 |
| `rate(redis_commands_processed_total[5m])` | OPS |
| `redis_memory_max_bytes` | 最大内存（若配置） |

### 2.5 RocketMQ（rocketmq-exporter）

| 指标 | 说明 |
|------|------|
| `rocketmq_producer_tps` | Broker 生产 TPS |
| `rocketmq_consumer_tps` | Broker 消费 TPS |
| `rocketmq_group_diff` | Consumer Group 消费滞后（diff） |
| `rocketmq_topic_offset` | Topic 位点 |

Exporter 需配置 `rocketmq.config.namesrvAddr=host.docker.internal:9877`（NameServer 宿主机端口）。

### 2.6 ai-pay-settle 应用（Micrometer）

**业务指标（已有）**

| 指标 | 类型 | 说明 |
|------|------|------|
| `pay_mq_consume_total` | Counter | MQ 消费次数 |
| `pay_mq_produce_total` | Counter | MQ 生产次数 |
| `pay_outbox_pending` | Gauge | Outbox 积压 |
| `pay_clearance_pending` | Gauge | 清算 Pending |
| `pay_mq_circuit_open` | Gauge | 积压熔断 |
| `pay_http_request_total` | Counter | HTTP 接入 |

**运行时指标（Actuator 内置）**

| 指标 | 说明 |
|------|------|
| `process_cpu_usage` | JVM 进程 CPU |
| `system_cpu_usage` | 系统 CPU |
| `jvm_memory_used_bytes` | JVM 堆/非堆 |
| `jvm_gc_pause_seconds_*` | GC 停顿 |
| `hikaricp_connections_active` | 连接池活跃连接 |

**新增 MQ 基础设施指标**

| 指标 | 类型 | 说明 |
|------|------|------|
| `pay_mq_consumer_lag` | Gauge | 各 Group Topic 消费滞后 |
| `pay_mq_dlq_count` | Gauge | 各 Group DLQ 消息数 |

---

## 3. Grafana 看板

| UID | 名称 | 内容 |
|-----|------|------|
| `pay-settle-overview` | Pay-Settle Overview | 业务 TPS、积压、熔断、消费 P99（已有，增强 JVM CPU） |
| `pay-infra-fullstack` | 全方位基础设施 | 主机/容器/MySQL/Redis/MQ 资源 |
| `pay-app-runtime` | 应用运行时 | JVM CPU/内存/GC/线程/HikariCP |

默认首页：**全方位基础设施**（`pay-infra-fullstack.json`）。

---

## 4. Prometheus 告警规则

文件：`docs/docker/monitoring/prometheus/alerts.yml`

| 告警 | 条件 | 级别 |
|------|------|------|
| HostHighCpu | CPU > 85% 5m | warning |
| HostLowMemory | 可用内存 < 10% | warning |
| MysqlHighConnections | 连接使用率 > 80% | warning |
| RedisHighMemory | used/max > 85% | warning |
| RocketMqConsumerLag | group_diff > 10000 | critical |
| PayAppDown | up{job="pay-settle"} == 0 | critical |
| PayOutboxBacklog | pay_outbox_pending > 5000 | warning |
| PayMqCircuitOpen | pay_mq_circuit_open == 1 | critical |

> 本地环境未部署 AlertManager，规则写入 Prometheus 供 UI 查看；生产可接 AlertManager + 企微/邮件。

---

## 5. 部署与使用

### 5.1 前置条件

以下服务需在宿主机运行（端口映射到本机）：

| 服务 | 容器名 | 端口 |
|------|--------|------|
| MySQL | mysql8 | 3306 |
| Redis | redis7 | 6379 |
| RocketMQ NameServer | rmq-namesrv | 9877→9876 |
| pay-app | 进程 | 18089 |

### 5.2 启动监控栈

```bash
./docs/docker/monitoring/init-monitoring.sh
```

### 5.3 启动应用（暴露指标）

```bash
java -jar pay-app/target/pay-app-*.jar \
  --spring.profiles.active=mysql,sharding,mq
```

### 5.4 访问

| 服务 | URL | 账号 |
|------|-----|------|
| Grafana | http://127.0.0.1:3000 | admin / admin |
| Prometheus | http://127.0.0.1:9090 | — |
| 应用指标 | http://127.0.0.1:18089/actuator/prometheus | — |

### 5.5 验证抓取目标

```bash
# Prometheus 所有 target 应为 UP
curl -s http://127.0.0.1:9090/api/v1/targets | jq '.data.activeTargets[] | {job, health}'

# 抽样指标
curl -s http://127.0.0.1:18089/actuator/prometheus | grep -E 'pay_mq_consumer_lag|process_cpu'
curl -s http://127.0.0.1:9090/api/v1/query?query=mysql_global_status_threads_connected
```

---

## 6. 目录结构

```
docs/docker/monitoring/
├── docker-compose.yml          # Prometheus + Grafana + Exporters
├── init-monitoring.sh
├── prometheus/
│   ├── prometheus.yml          # scrape 配置
│   └── alerts.yml              # 告警规则
└── grafana/
    ├── provisioning/             # 数据源 + 看板自动加载
    └── dashboards/
        ├── pay-settle-overview.json
        ├── pay-infra-fullstack.json
        └── pay-app-runtime.json

pay-mq/.../MqAdminMetricsCollector.java   # MQ lag/DLQ Gauge
```

---

## 7. 与既有方案关系

- 承接 [docs/monitor/系统监控方案.md](docs/monitor/系统监控方案.md) 的应用层指标设计
- 补齐 **基础设施 Exporter** 层（原方案仅 scrape pay-app）
- 应用内 `AlertService` 写 DB 告警与 Prometheus 告警互补，不互相替代
