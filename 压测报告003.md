# 压测报告 003 — 1000 商户 / 30 TPS × 30 分钟

> 日期：2026-07-15  
> 承接：[压测过程001.md](./压测过程001.md)、[压测过程002.md](./压测过程002.md)  
> 目标：清空库后种子商户 `1000001~1001000`，配置多种结算模式与分润关系，完整跑完 **30 TPS × 1800s**，给出端到端结果与优化方案。

---

## 1. 压测参数

| 项 | 值 |
|----|-----|
| 商户 | `1000001` ~ `1001000`（1000 户） |
| TPS / 时长 | 30 / 1800s（30 分钟） |
| 客户端 | 5 |
| 预计发送量 | ≈ 54,000 |
| Topic | `trade_pay_topic` / `PAY` |
| Profile | `mysql,sharding,mq` |
| JVM | `-Xms512m -Xmx1536m` |
| 熔断 | `--pay.mq.backlog.circuit-enabled=false` |
| Job 降频 | retry/compensate interval = 3600000ms |

编排脚本：`scripts/run-loadtest-003.sh`  
种子脚本：`scripts/seed-loadtest-merchants-003.sh`  
原始结果：`/tmp/loadtest003-result/`

---

## 2. 环境准备

### 2.1 文档阅读

已阅读项目内全部 `.md`（含架构/计费/清分/结算/分库分表/MQ 优化方案，以及压测 001/002），作为本轮参数与验收口径依据。

### 2.2 清空数据库

已清空：

- `pay_config` 业务表
- `pay_data_00~03` 全部分表
- `pay_settle`（遗留单库）
- Redis `FLUSHALL`
- RocketMQ 消费位点重置到最新（含 `%RETRY%*`）

### 2.3 商户种子（1000001~1001000）

| 配置项 | 设计 |
|--------|------|
| 结算模式 `settle_mode` | 1T1 / 2D0 / 3D1 / 4H0 / 5S0 各 **200** 户轮转 |
| 分润关系 | `none` / `L1_only` / `L1_L2` / `L1_L2_PARTNER` 各 **250** 户 |
| 代理池 | 一级 `210000+`、二级 `220000+`、合伙人 `310000+` 多组轮转 |
| 中间户 | 按 `merchant_id%16` 写入 16 张物理 `merchant_settle_account_*` |
| 计费规则 | 9 条生效规则：平台默认/业务线差异、L1/L2 比例、合伙人阶梯与固定比例 |

校验：`merchant_profile=1000`，`settle_account=1000`，`fee_share_rule=9`，16 分片均有商户。

压测消息在区间模式下 **不填 agentId**，分润完全由 `agent_merchant_relation` 驱动。

---

## 3. 过程时间线

| 时间 | 事件 |
|------|------|
| 02:56:35 | 编排启动，重置 MQ 位点 |
| 02:57:37 | pay-app Health UP；Consumer ONLINE |
| 02:57:52 | MQ 压测开始；`BASE_ROUTES=0` |
| 02:58~03:22 | routes 持续增长；accessDiff≈0；appAlive=1 |
| 03:24 / 03:27 | 两次短暂 HEALTH_DOWN（进程仍在），随后恢复 |
| ~03:27:50 | 发送结束 `LOADTEST_EXIT=0` |
| +90s drain | 最终 `END_ROUTES=764` |

首轮曾因 watchdog 误杀应用在 ~17 分钟中断；已改为「仅进程消失才重启 + Producer 独立 nohup」后完整跑满 30 分钟。

---

## 4. 发送侧结果（达标）

| 指标 | 值 |
|------|-----|
| 总请求 | 53,999 |
| 成功 / 失败 | 53,999 / 0 |
| 成功率 | **100%** |
| 实际 TPS | **30.00** |
| 耗时 | 1,799,910 ms |
| 平均延迟 | 1.77 ms |
| P50 / P95 / P99 | 2 / 2 / **3** ms |
| 最大延迟 | 215 ms |

结论：**MQ 发送侧 30 TPS × 30 分钟完全达标。**

---

## 5. 端到端与分库分表结果

### 5.1 总量（商户 1000001~1001000）

| 指标 | 数值 | 说明 |
|------|------|------|
| bill_route 新增 | **764** | 相对发送 ≈1.4% |
| trade_bill 合计 | **764** | 与 route 一致 |
| clearance_task SUCCESS | **734** | 落库单约 96% 清算成功 |
| outbox dispatched (status=1) | **500** | 入账投递部分完成 |
| wait_balance>0 商户 | **1000** | 全商户均有入账痕迹 |
| 涉及商户数（bill_route） | **571** | 覆盖过半商户 |
| 涉及分片 | **16/16** | 全覆盖 |

### 5.2 清算/分润覆盖

**结算模式（有账单商户）：**

| settle_mode | 商户数 | 账单数 |
|------------:|-------:|-------:|
| 1 T1 | 116 | 163 |
| 2 D0 | 108 | 138 |
| 3 D1 | 109 | 164 |
| 4 H0 | 122 | 150 |
| 5 S0 | 116 | 149 |

**分润关系：**

| rel_type | 商户数 | 账单数 |
|----------|-------:|-------:|
| none | 180 | 237 |
| L1_only | 126 | 202 |
| L1_L2 | 152 | 203 |
| L1_L2_PARTNER | 113 | 122 |

结论：多种结算模式与分润关系均被真实打到；链路可跑通。

### 5.3 分片分布（bill_route）

| shard | routes | merchants |
|------:|-------:|----------:|
| 0 | 35 | 32 |
| 1 | 5 | 5 |
| 2 | 115 | 63 |
| 3 | 48 | 38 |
| 4 | 30 | 29 |
| 5 | 70 | 60 |
| 6 | 10 | 10 |
| 7 | 97 | 63 |
| 8 | 17 | 16 |
| 9 | 85 | 62 |
| 10 | 8 | 7 |
| 11 | 16 | 16 |
| 12 | 40 | 36 |
| 13 | 77 | 53 |
| 14 | 69 | 46 |
| 15 | 42 | 35 |

路由规则正确（落入 `pay_data_00~03` × `*_0~3`），但分片吞吐不均，热点分片锁冲突更重。

---

## 6. 问题观察

| 现象 | 数据 | 影响 |
|------|------|------|
| Lock wait timeout | **3984** | 端到端主瓶颈 |
| Data too long for column `error_msg` | **1400** | 失败写库二次异常，放大重试 |
| trade pay consume failed | **1394** | 接入消费失败/重试 |
| clearance failed | **226** | 清算失败 |
| Connection timeout | **31** | 连接池偶发耗尽 |
| HEALTH_DOWN 闪断 | 2 次 | DB 压力下 actuator 短暂不可用 |
| Listener 线程 | 注解 `consumeThreadMax=20`，且 `consumeThreadNumber` 默认 **min=20** | YAML 的 8 未生效，高并发放大锁冲突 |
| PAY/REFUND 同 Group | `pay-access-consumer` Count=2 | 同组双订阅，位点与并行度难控 |

相对 002（100 商户 / 10 分钟 / 新增 route 307）：本轮商户×10、时长×3，落库绝对量提升到 764，但相对发送比仍约 **1.4%**，说明瓶颈仍在消费写库而非发送。

---

## 7. 优化方案（按优先级）

### P0 — 必须立刻做

1. **消费线程真正读配置**  
   Listener 实现 `RocketMQPushConsumerLifecycleListener`，在 `prepareStart` 中按 `MqConsumerProperties` 同时设置 `consumeThreadMin=Max=N`（分片联调 N=4~8）。  
   否则 YAML `access-thread-max: 8` 无效，实际仍是 min=20。

2. **截断 `error_msg`（≤512）**  
   写 `clearance_task.error_msg` 前截断；避免 `Data too long` 导致失败路径再次失败、无意义重试。

3. **PAY / REFUND 拆分 Consumer Group**  
   新增 `pay-access-refund-consumer`，消除同组双订阅干扰。

### P1 — 吞吐提升

4. **压测 Job 开关**  
   `pay.loadtest.pause-jobs=true` 时跳过 `ClearanceRetryJob` / `OutboxCompensateJob` 等历史扫描，减少与热路径抢锁。

5. **账单缺失改为不可重试**  
   `executeTask` 中 `findByBillNo.orElseThrow` 改为捕获后抛 `NonRetryableException`（或标记 DEAD），避免脏消息打满重试队列。

6. **缩短清算事务**  
   `activateWaitingRefunds` 已偏后置，继续确认计费/清分写库是否可拆；避免在同一 `@DSTransactional` 内做可延迟工作。

### P2 — 可观测与验收

7. **端到端看板**  
   对齐：发送量 / bill_route / clearance SUCCESS / outbox dispatched / credited merchants。

8. **通过标准建议**  
   新增 route / 发送量 ≥ 95%，clearance SUCCESS / trade_bill ≥ 95%。

---

## 8. 一句话结论

**发送侧 30 TPS×30 分钟 100% 成功（实际 TPS=30.00，P99=3ms）；1000 商户、5 种结算模式、4 类分润关系与 16 分片均被覆盖，清算链路可跑通。**  
端到端新增落库 764/53999，主因仍是 InnoDB 锁等待 + `error_msg` 过长二次失败 + 消费线程（min=20）未按 YAML 降并发。下文按 §7 优化方案改代码。

---

## 9. 本轮已落地的代码优化（对应 §7）

| 优化项 | 改动 |
|--------|------|
| P0 消费线程读配置 | 各 Listener 实现 `RocketMQPushConsumerLifecycleListener`，`prepareStart` 中 `min=max=MqConsumerProperties` |
| P0 error_msg 截断 | `ClearanceTaskServiceImpl.truncateError`，长度 ≤500 |
| P0 PAY/REFUND 拆 Group | 新增 `MqConsumerGroups.ACCESS_REFUND` |
| P1 压测 Job 开关 | `pay.loadtest.pause-jobs`；ClearanceRetry/OutboxCompensate/SplitCompensate/BillRouteCompensate 跳过 |
| P1 账单缺失不可重试 | 缺单标记 DEAD + `NonRetryableException` |

## 10. 相关文件

| 路径 | 用途 |
|------|------|
| `scripts/seed-loadtest-merchants-003.sh` | 1000 商户 + 结算/分润/规则种子 |
| `scripts/run-loadtest-003.sh` | 30 分钟压测编排 |
| `pay-test/.../TradePayMessage.java` | 区间压测不填代理，走 DB 关系 |
| `pay-app/.../ShardTableInitializer.java` | 启动种子 1000001~1001000 中间户 |
| `pay-mq/.../MqConsumerThreadSupport.java` | 统一设置消费线程 min/max |
| `/tmp/loadtest003-result/` | 原始日志与 verify 输出 |
