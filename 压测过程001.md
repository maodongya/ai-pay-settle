# 压测过程 001 — 分库分表 + MQ 整体压测

> 日期：2026-07-15  
> 目标：建立商户 `10001~10100`，在 `mysql,sharding,mq` 下以 **30 TPS / 10 分钟** 压测全链路，验证分库分表路由与清算结算流水线。

---

## 1. 压测目标

| 项 | 值 |
|----|-----|
| 商户 | `10001` ~ `10100`（100 个） |
| 时长 | 600 秒（10 分钟） |
| MQ 发送 TPS | 30 |
| 预计消息量 | ≈ 18,000 |
| Topic | `trade_pay_topic` / Tag `PAY` |
| Profile | `mysql,sharding,mq` |
| 验证点 | 商户均匀落到 4 库 × 4 表（16 分片）；接入→清算→清分→入账链路可跑通 |

分片规则：`shard_id = merchant_id % 16`，`db = shard_id / 4`，`table_suffix = shard_id % 4`。

---

## 2. 环境准备

### 2.1 依赖服务（Docker）

- MySQL：`mysql8`（`pay_config` + `pay_data_00~03`）
- RocketMQ：`rmq-namesrv`（宿主机 `9877`）、`rmq-broker`
- Redis / Prometheus / Grafana（可选监控）

### 2.2 应用启动

```bash
mvn -pl pay-app -am package -DskipTests

java -Xmx1024m -jar pay-app/target/pay-app-1.0.0-SNAPSHOT.jar \
  --spring.profiles.active=mysql,sharding,mq \
  --pay.mq.backlog.circuit-enabled=false
```

健康检查：`curl http://127.0.0.1:18089/actuator/health`

### 2.3 商户种子（10001~10100）

脚本：`scripts/seed-loadtest-merchants.sh`

写入内容：

1. **`pay_config`**：`merchant_profile` / `merchant_contract` / `agent_merchant_relation`
2. **`pay_data_XX.merchant_settle_account_Y`**：按 `merchant_id` 路由到物理分表（中间户入账必需）

校验结果（首次种子）：

- `merchant_profile`：100
- `merchant_settle_account`：100，分布在 16 张物理表（每分片约 6~7 户）

应用侧同步增强：`ShardTableInitializer` 启动时也会 `INSERT IGNORE` 种子 `10001~10100` 的中间户。

### 2.4 MQ 历史积压清理（压测前必做）

此前环境存在大量历史积压（access ~5 万、calc ~1 万、RETRY 队列），会打满连接池并触发熔断。压测前将消费位点重置到最新：

```bash
docker exec rmq-broker sh -c '
cd /home/rocketmq/rocketmq-4.9.6
for pair in \
  "pay-access-consumer:trade_pay_topic" \
  "pay-access-consumer:%RETRY%pay-access-consumer" \
  "pay-calc-consumer:clearance_task_topic" \
  "pay-calc-consumer:%RETRY%pay-calc-consumer" \
  "pay-settlement-consumer:settle_amount_topic"
do
  g=${pair%%:*}; t=${pair#*:}
  sh bin/mqadmin resetOffsetByTime -n rmq-namesrv:9876 -g "$g" -t "$t" -s now -f true
done
'
```

可选：将非压测商户的历史 `clearance_task`（PENDING/RUNNING/FAILED）标记为 DEAD，减轻 `ClearanceRetryJob` 扫库压力。

---

## 3. 压测工具改造

原 `pay-test` MQ 压测只支持**单个** `merchant-id`，无法覆盖 100 商户分片。本次改造：

| 改动 | 说明 |
|------|------|
| `MqLoadTestConfig` | 新增 `merchant-id-start` / `merchant-id-end`，`resolveMerchantId(seq)` 轮询 |
| `TradePayMessage` | `merchantId` 按区间轮询；`billNo` 加毫秒前缀避免多轮压测幂等撞车 |
| `run-mq-load-test.sh` | 支持环境变量 `MERCHANT_ID_START` / `MERCHANT_ID_END` |

执行命令：

```bash
NAME_SERVER=127.0.0.1:9877 \
CLIENTS=5 TPS=30 DURATION=600 \
MERCHANT_ID_START=10001 MERCHANT_ID_END=10100 \
./pay-test/run-mq-load-test.sh --report-interval-seconds=60
```

分片结果核对脚本：`scripts/verify-shard-loadtest.sh`。

---

## 4. 压测过程与轮次

### 4.1 第 1 轮（完整 10 分钟，发送侧成功）

**结果（Producer）：**

| 指标 | 值 |
|------|-----|
| 总请求 | 18,000 |
| 成功 / 失败 | 18,000 / 0 |
| 实际 TPS | 29.84 |
| P99 | 6 ms |
| 平均延迟 | 1.82 ms |

**消费侧问题：**

- Hikari 内层池（`pay_data_*`）出现 `Connection is not available, request timed out after 30005ms`
- `trade pay consume failed` 大量出现
- 端到端落库远少于发送量（当时约数百单级别）
- 根因叠加：历史积压 + 消费线程偏高（注解 `consumeThreadMax=20`）+ 分片广播查询/长事务

日志参考：`/tmp/mq-loadtest-30tps-10m.log`

### 4.2 第 2 轮（中断：幂等撞单）

第二轮复用 `billNo = MQ{client}-{seq}`，与第一轮重复，`submitBill` 幂等直接返回，几乎不新增账单。

**修复：** `billNo = MQ{currentTimeMillis}-{client}-{seq}`。

### 4.3 第 3 轮及后续（应用掉线 / 锁等待）

现象：

- 应用进程偶发退出或 MQ Consumer 心跳超时变 OFFLINE，积压迅速回升到数千~上万
- MySQL `PROCESSLIST` 大量 `INSERT INTO trade_bill_*` / `outbox_message_*` 长时间卡在 `Execute/update`（锁等待 19~35s）
- `clearance failed`：`Lock wait timeout exceeded`
- 发送侧仍可稳定 30 TPS，但端到端 TPS 极低

期间曾短暂观察到：

- Access Diff≈0（跟得上发送）
- 新单号格式 `MQ1784...` 已写入 `bill_route`
- 但新增路由速率远低于 30 TPS（锁争用导致吞吐塌陷）

---

## 5. 问题根因与优化点

### 5.1 已落地的优化

#### （1）压测商户与分片种子

- 新增 `scripts/seed-loadtest-merchants.sh`
- `ShardTableInitializer` 自动种子 `10001~10100` 中间户到正确物理表
- 保证「接入校验 + 结算入账」两端都有数据

#### （2）MQ 压测支持商户区间

- 100 商户轮询，真实打到 16 个分片
- 唯一 `billNo`，避免多轮压测被幂等吞掉

#### （3）连接池与消费并发（配置）

`shardingsphere.yaml`：各 `ds_0~3` `maximumPoolSize` **20 → 40**

`application-sharding.yml`：

```yaml
pay.mq.consumer:
  access-thread-max: 8
  calc-thread-max: 8
  settlement-thread-max: 8
  settlement-payment-thread-max: 4
```

> 注意：各 Listener 注解里仍硬编码 `consumeThreadMax = 20`（RocketMQ 默认 min=20），YAML 项尚未完全生效到注解；后续应改为读取 `MqConsumerProperties`。

压测时关闭积压熔断：`--pay.mq.backlog.circuit-enabled=false`。

#### （4）接入幂等避免分片广播（代码）

**问题：** 新单尚无 `bill_route` 时，`tradeBillRepository.findByBillNo` 缺少 `merchant_id`，ShardingSphere **广播扫 16 张物理表**，在高并发下放大锁与连接压力。

**优化：** `BillAccessServiceImpl.submitBill` 先查 `pay_config.bill_route`（单库主键），无路由再走创建；有路由再带分片键读 `trade_bill`。

#### （5）事务与 MQ 发送解耦（代码）

**问题：** `@DSTransactional` 覆盖「落库 + `createTask` + `triggerClearance`(MQ publish/同步清算)」整段逻辑。MQ/下游耗时长时，InnoDB 行锁/间隙锁长时间不释放，表现为：

- `INSERT trade_bill_*` 卡在 `update` 数十秒
- Consumer 线程池被占满 → 心跳失败 → Group OFFLINE → 积压雪崩

**优化：**

- `persistNewBill`：仅 DB（`@DSTransactional`）
- `triggerClearance`：事务提交之后再调用
- 通过 `@Lazy` 自注入保证事务注解经代理生效

### 5.2 仍建议继续优化（未完全闭环）

| 优先级 | 项 | 说明 |
|--------|----|------|
| P0 | Listener `consumeThreadMax` 改为读配置 | 注解硬编码 20，与 YAML 不一致；分片联调建议 4~8 |
| P0 | 压测前自动 reset offset + 清理非压测 pending 任务 | 写入一键脚本，避免人工遗漏 |
| P1 | Job 与压测隔离 | `ClearanceRetryJob` / `OutboxCompensateJob` 扫历史数据会与压测抢锁；可增加「压测开关」或缩小扫描范围 |
| P1 | `error_msg` 字段过短 | 失败写库时出现 `Data too long for column 'error_msg'` |
| P1 | `ClearanceTaskServiceImpl` 对缺失账单 `Optional.orElseThrow` | 历史脏消息导致 `NoSuchElementException` → 重试/DLQ |
| P2 | 端到端成功率看板 | 以 `bill_route` / `clearance_task SUCCESS` / `outbox status=1` / `wait_balance>0` 对齐发送量 |
| P2 | 应用保活 | 长压测时用 systemd/脚本 watchdog，避免进程退出后 Producer 空跑 |

---

## 6. 发送侧结论 vs 端到端结论

### 6.1 MQ 发送（已达标）

在 10 分钟、30 TPS、商户 `10001~10100` 条件下：

- 发送成功率 **100%**
- 实际 TPS **≈ 29.84**
- 延迟：P50≈2ms，P99≈6ms

### 6.2 端到端（分库分表链路，需在优化后复测）

首轮及中途轮次中，因连接池耗尽、锁等待、应用短暂 OFFLINE、幂等撞单等，**落库量显著低于发送量**。  
在完成「幂等改查 bill_route + 事务外触发清算 + 池化调优 + 清空积压」后，应重新跑一轮完整 10 分钟，并用下列口径验收：

```bash
# 发送结束后等待管道排空，再执行
./scripts/verify-shard-loadtest.sh

# 期望（理想）
# - bill_route 新增 ≈ 18000（或接近，允许少量消费失败）
# - 100 个 merchant 均有数据
# - 16 个 shard（merchant_id%16）均有账单
# - clearance_task SUCCESS 接近 bill 数
# - outbox dispatched / wait_balance>0 商户数合理上升
```

复测命令（推荐）：

```bash
# 1) 重置位点 + 启动
# 2) 压测
NAME_SERVER=127.0.0.1:9877 CLIENTS=5 TPS=30 DURATION=600 \
MERCHANT_ID_START=10001 MERCHANT_ID_END=10100 \
./pay-test/run-mq-load-test.sh --report-interval-seconds=60

# 3) 核对分片
./scripts/verify-shard-loadtest.sh
```

---

## 7. 相关文件清单

| 路径 | 用途 |
|------|------|
| `scripts/seed-loadtest-merchants.sh` | 种子 100 商户 + 分片中间户 |
| `scripts/verify-shard-loadtest.sh` | 压测后分片分布核对 |
| `pay-test/run-mq-load-test.sh` | MQ 压测入口 |
| `pay-test/.../MqLoadTestConfig.java` | 商户区间配置 |
| `pay-test/.../TradePayMessage.java` | 轮询商户 + 唯一单号 |
| `pay-access/.../BillAccessServiceImpl.java` | 幂等查路由 + 事务外触发清算 |
| `pay-app/.../ShardTableInitializer.java` | 启动种子中间户 |
| `pay-app/.../application-sharding.yml` | 分片池 / 消费线程 / 熔断 |
| `pay-app/.../shardingsphere.yaml` | 内层数据源池大小 |

---

## 8. 一句话总结

**发送侧 30 TPS×10 分钟已稳定跑满；分库分表端到端曾被「历史积压 + 分片广播幂等查询 + 事务内发 MQ」拖垮。**  
已完成商户种子、压测工具区间轮询、连接池调优、接入幂等与事务边界优化；建议用同一参数再跑一轮完整 10 分钟，并以 `verify-shard-loadtest.sh` 作为分片验收标准。
