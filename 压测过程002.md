# 压测过程 002 — 分库分表 + MQ 整体压测（复测）

> 日期：2026-07-15  
> 承接：[压测过程001.md](./压测过程001.md)  
> 目标：在已落地优化（商户种子、区间轮询、幂等查路由、事务外触发清算、连接池调优）后，**完整跑完** 30 TPS × 10 分钟，并核对分库分表落库。

---

## 1. 本轮参数

| 项 | 值 |
|----|-----|
| 商户 | `10001` ~ `10100`（100 户，已种子） |
| TPS / 时长 | 30 / 600s |
| 客户端 | 5 |
| 预计发送量 | 18,000 |
| Topic | `trade_pay_topic` / `PAY` |
| Profile | `mysql,sharding,mq` |
| JVM | `-Xms512m -Xmx1536m` |
| 熔断 | `--pay.mq.backlog.circuit-enabled=false` |

启动与压测入口：

```bash
# 应用（与压测同属一个长生命周期 shell，避免进程被提前回收）
java -Xms512m -Xmx1536m -jar pay-app/target/pay-app-1.0.0-SNAPSHOT.jar \
  --spring.profiles.active=mysql,sharding,mq \
  --pay.mq.backlog.circuit-enabled=false

# 压测
NAME_SERVER=127.0.0.1:9877 CLIENTS=5 TPS=30 DURATION=600 \
MERCHANT_ID_START=10001 MERCHANT_ID_END=10100 \
./pay-test/run-mq-load-test.sh --report-interval-seconds=60
```

编排脚本（可选）：`scripts/run-loadtest-002.sh`  
原始结果目录：`/tmp/loadtest002-result/`

---

## 2. 压测前准备

1. `mvn -pl pay-app,pay-test -am package -DskipTests`（含 001 中的代码优化）
2. 确认 `pay_config.merchant_profile` 中 `10001~10100` = **100**
3. RocketMQ 消费位点重置到最新（含 `%RETRY%*`）
4. 非压测商户历史 `clearance_task`（PENDING/RUNNING/FAILED）标记 DEAD，降低 Job 抢锁
5. 应用拉起后确认 Consumer ONLINE、Diff≈0

基线：`bill_route`（商户 10001~10100）= **397**（含 001 轮残留）

---

## 3. 过程时间线

| 时间 | 事件 |
|------|------|
| 01:50:55 | 启动 pay-app（PID 37737） |
| 01:51:19 | Health UP；access/calc/settlement Consumer ONLINE |
| 01:51:47 | MQ 压测开始 |
| 01:52:51 | SAMPLE：accessDiff≈1519（启动瞬间积压），routes 尚未增长 |
| 01:53:56 | SAMPLE：routes=435（+38），accessDiff≈0（消费追上） |
| 01:55~01:58 | routes 持续增长至 614（+217）；accessDiff 维持数十~百级 |
| 02:00:02 | SAMPLE 曾出现短暂 HEALTH_DOWN（随后恢复） |
| 02:01:07 | routes=667（+270） |
| 02:02:01 | **压测发送结束 exit=0** |
| +60s drain | 最终 routes=704（**本轮新增 +307**） |

期间应用与 Producer 在同一 shell 内保活；空载稳定性测试曾验证进程可稳定 >90s。此前 `nohup+disown` 方式下应用易被环境回收，本轮改为长生命周期 shell 绑定。

---

## 4. 发送侧结果（达标）

| 指标 | 值 |
|------|-----|
| 总请求 | 18,000 |
| 成功 / 失败 | 18,000 / 0 |
| 成功率 | 100% |
| 实际 TPS | **30.00** |
| 耗时 | 599,929 ms |
| 平均延迟 | 1.79 ms |
| P50 / P95 / P99 | 2 / 3 / **5** ms |
| 最大延迟 | 166 ms |

结论：**MQ 发送侧 30 TPS × 10 分钟完全达标。**

---

## 5. 端到端与分库分表结果

### 5.1 总量（商户 10001~10100）

| 指标 | 数值 | 说明 |
|------|------|------|
| bill_route 基线 → 结束 | 397 → 704 | 本轮新增 **307** |
| trade_bill 物理表合计 | **705** | 与 route 基本一致 |
| clearance_task SUCCESS | **681** | 新增单大部分完成清算 |
| outbox dispatched (status=1) | **391** | 入账消息投递部分完成 |
| 有 `wait_balance>0` 的商户 | **100** | 100 商户均有入账痕迹 |
| 涉及商户数 | **100** | 区间全覆盖 |
| 涉及分片 | **16/16** | `merchant_id % 16` 全覆盖 |

相对发送 18,000 条，本轮新增落库约 **307**（约 1.7%）。主要瓶颈仍是 DB 锁等待（见 §6），大量消费重试/失败未形成新单；但**分片路由与全商户覆盖已验证通过**。

### 5.2 分片分布（bill_route / trade_bill）

| shard | routes | merchants | 物理表 |
|------:|-------:|----------:|--------|
| 0 | 34 | 6 | `pay_data_00.trade_bill_0` = 35 |
| 1 | 97 | 7 | `pay_data_00.trade_bill_1` = 97 |
| 2 | 70 | 7 | `pay_data_00.trade_bill_2` = 70 |
| 3 | 63 | 7 | `pay_data_00.trade_bill_3` = 63 |
| 4 | 55 | 7 | `pay_data_01.trade_bill_0` = 55 |
| 5 | 22 | 6 | `pay_data_01.trade_bill_1` = 22 |
| 6 | 51 | 6 | `pay_data_01.trade_bill_2` = 51 |
| 7 | 20 | 6 | `pay_data_01.trade_bill_3` = 20 |
| 8 | 31 | 6 | `pay_data_02.trade_bill_0` = 31 |
| 9 | 47 | 6 | `pay_data_02.trade_bill_1` = 47 |
| 10 | 32 | 6 | `pay_data_02.trade_bill_2` = 32 |
| 11 | 25 | 6 | `pay_data_02.trade_bill_3` = 25 |
| 12 | 25 | 6 | `pay_data_03.trade_bill_0` = 25 |
| 13 | 36 | 6 | `pay_data_03.trade_bill_1` = 36 |
| 14 | 25 | 6 | `pay_data_03.trade_bill_2` = 25 |
| 15 | 71 | 6 | `pay_data_03.trade_bill_3` = 71 |

**分库分表结论：** 数据已按 `merchant_id % 16` 正确落入 `pay_data_00~03` × `*_0~3`，100 商户、16 分片均有流量。

### 5.3 clearance SUCCESS 分表（摘要）

各物理 `clearance_task_*` 均有 SUCCESS，合计 **681**；与 trade_bill 705 接近，说明落库单大多走完清算。

---

## 6. 问题与观察

| 现象 | 数据/说明 |
|------|-----------|
| Lock wait timeout | 应用日志约 **1192** 次 |
| Connection timeout | 约 **5** 次（较 001 轮大幅下降） |
| 短暂 Health DOWN | t≈420s 采样到一次，随后恢复 |
| 端到端落库率低 | 新增 route ≈307 / 发送 18000 |
| Consumer 注解线程 | Listener 仍硬编码 `consumeThreadMax=20`，高并发放大锁冲突 |
| Job 补偿 | `OutboxCompensateJob` 等仍扫历史单，与压测争用 |

相较 001：连接池打满已缓解；锁等待仍是端到端吞吐主因。

---

## 7. 相对 001 的改进验证

| 优化项 | 本轮表现 |
|--------|----------|
| 商户 10001~10100 种子 | 100 商户均有 route / 入账 |
| 压测商户区间轮询 | 16 分片全覆盖 |
| 唯一 billNo | 本轮无「整轮幂等撞车」问题 |
| 幂等先查 bill_route | 连接超时从千级降到个位数 |
| 事务外 triggerClearance | 应用可在压测期间维持（配合长生命周期 shell） |
| 内层池 40 + 位点重置 | 消费可追上（accessDiff 从千级回到数十） |

---

## 8. 后续建议（端到端贴近 30 TPS）

1. **Listener `consumeThreadMax` 改为读配置**，分片联调降到 4~8（注解硬编码 20 仍偏高）。
2. **压测开关**：暂停/缩小 `ClearanceRetryJob`、`OutboxCompensateJob` 历史扫描。
3. **缩短事务**：复核 `createTask`、清分写 outbox 是否仍持跨库长锁。
4. **验收口径**：以「新增 bill_route / 发送量」与「clearance SUCCESS / trade_bill」双比率作为通过标准（例如 >95%）。
5. **进程保活**：压测期间应用与 Producer 放同一常驻会话，或使用 launchd/systemd，避免 agent/shell 回收子进程。

---

## 9. 一句话结论

**本轮 MQ 发送 30 TPS×10 分钟 100% 成功（实际 TPS=30.00，P99=5ms）；分库分表路由正确（16 分片、100 商户全覆盖，`trade_bill` 分布在 `pay_data_00~03`）。**  
端到端新增落库约 307/18000，受 InnoDB 锁等待制约；清算成功 681、100 商户均有入账，链路可跑通但吞吐仍需继续降并发与收紧事务。
